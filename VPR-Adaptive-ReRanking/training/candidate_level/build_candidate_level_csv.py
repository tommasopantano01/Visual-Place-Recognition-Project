"""
training/candidate_level/build_candidate_level_csv.py — Costruisce
candidate_level.csv: un record per coppia (query, candidato) unendo i risultati
di retrieval (L2, rank) e di image matching (num_inliers) sul set di
calibrazione (train/val), con l'etichetta is_positive. E' l'input dei file in
validation/. Le colonne sono descritte in master.csv.
[VUOTO — codice da incollare.]
"""
